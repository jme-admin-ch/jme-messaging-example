jeap:
  messaging:
    kafka:
      bootstrapServers: "broker:29092"
      schemaRegistryUrl: "http://schema-registry:8081"
      securityProtocol: PLAINTEXT

spring:
  datasource:
    url: "jdbc:postgresql://jeap-error-handling-service-db-local:5432/jeap-error-handling-service-db-local"
