jeap:
  messaging:
    kafka:
      bootstrapServers: "broker:29092"
      schemaRegistryUrl: "http://schema-registry:8081"
      securityProtocol: PLAINTEXT
      set-compatibility-mode-none: true

spring:
  datasource:
    url: "jdbc:postgresql://jme-receiverpublisher-outbox-local:5432/jme-receiverpublisher-outbox-db-local"
