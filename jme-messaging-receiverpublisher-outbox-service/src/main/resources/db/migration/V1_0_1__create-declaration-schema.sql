CREATE TABLE  declaration (
    id                     uuid                        PRIMARY KEY,
    text                   varchar                     NOT NULL,
    created_at             timestamp with time zone
);
